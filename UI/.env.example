# Full URL of the deployed backend (e.g. https://your-app.up.railway.app/api).
# Leave unset for local dev — vite.config.ts proxies /api and /uploads to
# http://localhost:3000 in that case.
VITE_API_URL=

VITE_PLAYFAB_TITLE_ID=149200
